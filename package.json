{
  "name": "ai-virtual-assistant",
  "version": "0.1.0",
  "private": true,
  "description": "Root workspace for the AI Virtual Assistant platform (Angular + Firebase Functions).",
  "scripts": {
    "install:all": "npm --prefix web ci && npm --prefix functions ci",
    "build:web": "npm --prefix web run build -- --configuration=production",
    "build:functions": "npm --prefix functions run build",
    "build": "npm run build:web && npm run build:functions",
    "emulators": "firebase emulators:start",
    "deploy": "firebase deploy",
    "deploy:hosting": "firebase deploy --only hosting",
    "deploy:functions": "firebase deploy --only functions",
    "deploy:rules": "firebase deploy --only firestore",
    "postdeploy:verify": "node scripts/postdeploy.js"
  }
}
